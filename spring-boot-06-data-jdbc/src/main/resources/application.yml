spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/test
    driver-class-name: com.mysql.jdbc.Driver
    #用于创建表，但是每次启动会执行一次，所以用了一次就删除掉吧
  #  schema:
     # - classpath:department.sql
